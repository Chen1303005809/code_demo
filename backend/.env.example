# LightRag 服务基础地址（必填，不含 /query/stream）
LLM_API_URL=http://localhost:9621

# Bearer Token（可选，LightRag 如不需要则留空）
LLM_API_KEY=

# 查询模式：mix / hybrid / local / global / naive
LLM_QUERY_MODE=mix

# 单次请求超时（毫秒）
LLM_TIMEOUT_MS=60000

# 并发槽位数
MAX_CONCURRENCY=3

# SQLite 数据库路径
DB_PATH=./data/history.db
